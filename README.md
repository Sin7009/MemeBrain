-----

## 🧠 MemeBrain: Бот-Мемогенератор, Триггерящийся Реакциями

MemeBrain — это Telegram-бот, который автоматически генерирует ситуативные мемы прямо в чате. Вместо того чтобы пытаться понять юмор, бот использует самое точное мерило «мемности» — реакцию самих пользователей.

### 🚀 Основные возможности (MVP)

Текущая версия бота реализует полный цикл генерации мема, инициированный действием пользователя:

1.  **Триггер по реакции**: Бот активируется, когда кто-то из участников чата ставит на сообщение одну из триггерных эмодзи: **🤡 (Клоун)** или **🔥 (Огонь)**.
2.  **Управление историей**: Бот хранит контекст (по умолчанию 10 последних сообщений). Это позволяет нейросети понять, почему именно это сообщение стало смешным.
3.  **Интеллектуальный анализ (LLM)**: Используя модель **GPT-4o mini** через OpenRouter, бот анализирует контекст диалога. Нейросеть генерирует подходящий верхний и нижний текст, а также формулирует точный поисковый запрос для шаблона.
4.  **Поиск шаблонов**: Для получения чистого изображения-шаблона используется **Google Custom Search JSON API**.
5.  **Генерация изображения**: Библиотека **Pillow** накладывает текст на шаблон в классическом мем-стиле (белый шрифт с черным контуром).

### ⚙️ Установка и запуск

Проект использует `aiogram` 3.x и Python 3.10+.

#### 1\. Установка зависимостей

Установите необходимые библиотеки:

```bash
pip install -r requirements.txt
```

*(Обратите внимание: зависимости для Face Swap (`insightface`, `onnxruntime`) закомментированы в `requirements.txt` и пока не требуются для MVP.)*

#### 2\. Конфигурация

Создайте файл `.env` в корне проекта и заполните его ключами.

| Переменная | Описание | Источник |
| :--- | :--- | :--- |
| `TELEGRAM_BOT_TOKEN` | Токен вашего Telegram-бота. | Telegram BotFather |
| `OPENROUTER_API_KEY` | Ключ API для OpenRouter. | OpenRouter |
| `Google Search_API_KEY` | Ключ API для Google Custom Search. | Google Cloud Console |
| `Google Search_CX_ID` | Идентификатор поисковой системы (Context ID). | Google Custom Search Engine |
| `OPENROUTER_MODEL` | Используемая модель (по умолчанию `openai/gpt-4o-mini`). | OpenRouter |
| `HISTORY_SIZE` | Количество сообщений, сохраняемых для контекста (по умолчанию 10). | Конфигурация |
| `FACE_SWAP_ENABLED` | **`False`** (для MVP). Установите в `True` для активации дипфейка (см. Roadmap). | Конфигурация |

#### 3\. Запуск

Запустите бота в асинхронном режиме:

```bash
python -m src.main
```

### 🛣️ Планы на будущее (Roadmap)

Это ключевые направления развития, которые сделают бота более персонализированным и надежным:

| Цель | Описание | Статус | Комментарии |
| :--- | :--- | :--- | :--- |
| **Интеграция Face Swap** | Реализовать замену лица на шаблоне мема лицом пользователя (дипфейк). | Запланировано | Требует установки `insightface`, `onnxruntime` и использования GPU для приемлемой скорости. |
| **Кэширование шаблонов** | Сохранение часто используемых шаблонов локально. | Запланировано | Ускорит генерацию и снизит зависимость от Google Search API. |
| **Модерация контента** | Дополнительная проверка генерируемого текста и/или шаблона на предмет токсичности (NSFW, hate speech) с помощью LLM или стороннего сервиса. | Запланировано | Критично для публичных ботов и репутационных рисков. |
| **Анализ изображений/стикеров** | Возможность делать мемы из сообщений, содержащих стикеры или изображения, используя VLM (Vision Language Model). | Исследование | Требует более дорогой модели (например, GPT-4o). |
| **Кастомные реакции** | Предоставление пользователям возможности настраивать свои триггерные эмодзи. | Ожидание | |

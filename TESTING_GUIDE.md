# 🧪 Руководство по тестированию MemeBrain

## 📋 Содержание
- [Обзор тестовой инфраструктуры](#обзор-тестовой-инфраструктуры)
- [Запуск тестов](#запуск-тестов)
- [Категории тестов](#категории-тестов)
- [CI/CD Pipeline](#cicd-pipeline)
- [Покрытие кода](#покрытие-кода)
- [Написание новых тестов](#написание-новых-тестов)

---

## 🏗️ Обзор тестовой инфраструктуры

MemeBrain имеет комплексную систему тестирования, покрывающую все аспекты работы бота:

### Структура тестов

```
tests/
├── conftest.py                          # Фикстуры и настройки pytest
├── test_config.py                       # Тесты конфигурации
├── test_utils.py                        # Тесты утилит
│
├── Unit Tests (Юнит-тесты)
│   ├── test_history.py                  # Тесты менеджера истории
│   ├── test_llm.py                      # Тесты LLM интеграции
│   ├── test_image_gen.py                # Тесты генерации изображений
│   ├── test_search.py                   # Тесты поиска
│   └── test_validation.py               # Тесты валидации
│
├── Extended Tests (Расширенные тесты)
│   ├── test_history_extended.py         # Расширенные тесты истории
│   ├── test_llm_extended.py             # Расширенные тесты LLM
│   └── test_handlers_extended.py        # Расширенные тесты обработчиков
│
├── Integration Tests (Интеграционные тесты)
│   └── test_handlers.py                 # Тесты обработчиков бота
│
├── E2E Tests (End-to-End тесты)
│   └── test_e2e_integration.py          # Полные пользовательские сценарии
│
├── Stress Tests (Стресс-тесты)
│   └── test_stress.py                   # Тесты производительности и нагрузки
│
└── Destructive Tests (Деструктивные тесты)
    ├── test_destructive_meme.py         # Граничные случаи генерации мемов
    └── test_destructive_text_wrapping.py # Граничные случаи текста
```

### Статистика

- **Всего тестов**: ~107
- **Покрытие кода**: >80%
- **Среднее время выполнения**: ~2-3 минуты

---

## 🚀 Запуск тестов

### Быстрый старт

```bash
# Установка зависимостей
pip install -r requirements.txt
pip install pytest-cov pytest-timeout pytest-asyncio pytest-mock

# Запуск всех тестов
pytest tests/
```

### Использование Makefile (рекомендуется)

```bash
# Показать справку
make help

# Установить зависимости
make install

# Запустить все тесты
make test

# Запустить тесты с покрытием
make test-coverage

# Запустить только быстрые тесты
make test-fast

# Полная проверка (линтинг + тесты)
make check
```

### Использование shell скрипта

```bash
# Сделать исполняемым (только первый раз)
chmod +x run_tests.sh

# Запустить все тесты
./run_tests.sh

# Запустить конкретную категорию
./run_tests.sh unit
./run_tests.sh e2e
./run_tests.sh stress

# Показать справку
./run_tests.sh help
```

### Прямое использование pytest

```bash
# Все тесты с verbose
pytest tests/ -v

# Только unit тесты
pytest tests/test_config.py tests/test_history.py -v

# E2E тесты
pytest tests/test_e2e_integration.py -v

# Стресс-тесты
pytest tests/test_stress.py -v

# С покрытием кода
pytest tests/ --cov=src --cov-report=html
```

---

## 📚 Категории тестов

### 1. 🧩 Unit Tests (Юнит-тесты)

**Цель**: Тестирование отдельных компонентов изолированно

**Команда запуска**:
```bash
make test-unit
# или
pytest tests/test_config.py tests/test_history.py tests/test_llm.py tests/test_image_gen.py tests/test_search.py tests/test_utils.py -v
```

**Покрывает**:
- ✅ Конфигурация и переменные окружения
- ✅ Менеджер истории сообщений
- ✅ LLM интеграция (OpenRouter)
- ✅ Генерация изображений с текстом
- ✅ Поиск изображений (Tavily API)
- ✅ Утилиты

**Примеры тестов**:
- `test_config_load_from_env` - загрузка конфигурации из .env
- `test_add_and_retrieve_message` - добавление и получение сообщений
- `test_create_meme_success` - успешная генерация мема

### 2. 🔗 Integration Tests (Интеграционные тесты)

**Цель**: Тестирование взаимодействия компонентов

**Команда запуска**:
```bash
make test-integration
# или
pytest tests/test_handlers.py tests/test_handlers_extended.py -v
```

**Покрывает**:
- ✅ Обработчики команд (/start, /help)
- ✅ Обработчики сообщений
- ✅ Обработчики реакций
- ✅ Генерация и отправка мемов
- ✅ Обработка ошибок в цепочке

**Примеры тестов**:
- `test_cmd_start` - обработка команды /start
- `test_reaction_handler_success` - успешная обработка реакции
- `test_generate_and_send_meme_llm_failure` - обработка ошибки LLM

### 3. 🎯 E2E Tests (End-to-End тесты)

**Цель**: Тестирование полных пользовательских сценариев

**Команда запуска**:
```bash
make test-e2e
# или
pytest tests/test_e2e_integration.py -v
```

**Покрывает**:
- ✅ Полный путь пользователя (от /start до мема)
- ✅ Групповые чаты с контекстом
- ✅ Естественные диалоги с реакциями
- ✅ Восстановление после ошибок
- ✅ Множественные пользователи и чаты
- ✅ Все 17 эмодзи-триггеров

**Примеры сценариев**:
- `test_complete_user_journey_private_chat` - полный путь в личном чате
- `test_natural_conversation_with_context` - естественный разговор
- `test_all_emoji_generate_appropriate_memes` - все триггеры
- `test_meme_debate_scenario` - дебаты с мемами

### 4. 💪 Stress Tests (Стресс-тесты)

**Цель**: Проверка производительности и стабильности под нагрузкой

**Команда запуска**:
```bash
make test-stress
# или
pytest tests/test_stress.py -v
```

**Покрывает**:
- ✅ 10+ команд одновременно
- ✅ 50+ параллельных запросов генерации
- ✅ 17 эмодзи-триггеров одновременно
- ✅ 100+ сообщений в истории
- ✅ 50 одновременных чатов
- ✅ Граничные случаи (пустые сообщения, длинные тексты)

**Примеры тестов**:
- `test_multiple_start_commands_concurrently` - множественные команды
- `test_all_emoji_triggers_simultaneously` - все триггеры сразу
- `test_history_with_large_messages` - большие сообщения
- `test_concurrent_meme_generation_multiple_chats` - параллельная генерация

### 5. 💥 Destructive Tests (Деструктивные тесты)

**Цель**: Тестирование граничных случаев и необычных ситуаций

**Команда запуска**:
```bash
make test-destructive
# или
pytest tests/test_destructive_*.py -v
```

**Покрывает**:
- ✅ Слишком маленькие изображения (1x1 пиксель)
- ✅ Невалидное содержимое файлов
- ✅ Слишком большие файлы
- ✅ Экстремально узкая ширина текста
- ✅ Некорректные параметры

**Примеры тестов**:
- `test_create_meme_crash_on_tiny_image` - крошечное изображение
- `test_create_meme_crash_on_non_image_content` - не-изображение
- `test_wrap_text_crash_on_narrow_width` - узкая ширина

---

## 🔄 CI/CD Pipeline

### GitHub Actions Workflow

Pipeline автоматически запускается при:
- Push в ветки `main` или `master`
- Создании Pull Request
- Ручном запуске (workflow_dispatch)

### Структура pipeline

```
┌─────────────────────────────────────────────────────────┐
│                     CI/CD PIPELINE                      │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
  ┌──────────┐      ┌─────────────┐    ┌──────────────┐
  │   Lint   │      │  Test Suite │    │   Coverage   │
  │ (ruff,   │      │ (3.10, 3.11,│    │   Report     │
  │  black)  │      │    3.12)    │    │  (Codecov)   │
  └──────────┘      └─────────────┘    └──────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                            ▼
                  ┌──────────────────┐
                  │ All Tests Passed │
                  │        ✅         │
                  └──────────────────┘
```

### Jobs

1. **Lint** (Code Quality Check)
   - Проверка кода с помощью `ruff`
   - Проверка форматирования с `black`
   - Продолжается даже при ошибках

2. **Test** (Test Suite)
   - Запускается на Python 3.10, 3.11, 3.12
   - Выполняет все категории тестов последовательно:
     - Unit Tests
     - Extended Tests
     - Integration Tests
     - E2E Tests
     - Stress Tests
     - Destructive Tests
   - Timeout: от 60 до 180 секунд на категорию

3. **Coverage** (Code Coverage Report)
   - Запускается только на Python 3.12
   - Генерирует отчет о покрытии
   - Загружает в Codecov (опционально)
   - Сохраняет артефакт coverage.xml

4. **All Tests Passed** (Итоговый статус)
   - Проверяет результаты всех jobs
   - Выводит итоговый статус

### Локальная эмуляция CI

```bash
# Эмуляция CI pipeline локально
make ci
```

---

## 📊 Покрытие кода

### Генерация отчета

```bash
# HTML отчет
pytest tests/ --cov=src --cov-report=html
# Откройте htmlcov/index.html в браузере

# Terminal отчет
pytest tests/ --cov=src --cov-report=term-missing

# XML отчет (для CI)
pytest tests/ --cov=src --cov-report=xml
```

### Использование Makefile

```bash
make test-coverage
```

### Настройки coverage

Конфигурация в `pytest.ini`:
```ini
[coverage:run]
source = src
omit = */tests/*, */test_*, */__pycache__/*

[coverage:report]
precision = 2
show_missing = True
exclude_lines =
    pragma: no cover
    def __repr__
    raise NotImplementedError
```

---

## ✍️ Написание новых тестов

### Структура теста

```python
"""
Краткое описание модуля тестов.
"""
import pytest
from unittest.mock import patch, AsyncMock

# Импорты тестируемых модулей
from src.services.my_service import MyService


@pytest.mark.asyncio
async def test_my_feature():
    """Описание теста."""
    # Arrange (Подготовка)
    service = MyService()
    test_data = "test"
    
    # Act (Действие)
    result = await service.do_something(test_data)
    
    # Assert (Проверка)
    assert result == expected_value
```

### Полезные фикстуры

```python
# conftest.py предоставляет:
# - mock_settings_env: Мок переменных окружения
# - mock_fonts: Мок для ImageFont (опциональный)

@pytest.fixture
def mock_message():
    """Фикстура для создания мок-сообщения."""
    msg = AsyncMock(spec=Message)
    msg.text = "Test"
    msg.chat.id = 123
    return msg
```

### Best Practices

1. **Используйте AsyncMock для async функций**
   ```python
   mock_func = AsyncMock(return_value="result")
   ```

2. **Группируйте связанные тесты в классы**
   ```python
   class TestHistoryManager:
       def test_add_message(self):
           pass
       
       def test_get_message(self):
           pass
   ```

3. **Используйте маркеры для категоризации**
   ```python
   @pytest.mark.slow
   @pytest.mark.integration
   async def test_slow_integration():
       pass
   ```

4. **Пишите описательные имена тестов**
   ```python
   # Хорошо
   def test_add_message_increments_history_count():
       pass
   
   # Плохо
   def test_1():
       pass
   ```

5. **Используйте моки для внешних зависимостей**
   ```python
   with patch('src.services.llm.openai_client') as mock_client:
       mock_client.chat.completions.create = AsyncMock(...)
   ```

### Добавление нового теста в pipeline

1. Создайте файл теста: `tests/test_new_feature.py`
2. Тест автоматически будет подхвачен pytest
3. Для добавления в конкретную категорию Makefile - отредактируйте `Makefile`

---

## 🛠️ Полезные команды

### Запуск конкретного теста

```bash
# Один тест
pytest tests/test_history.py::TestHistoryManager::test_add_and_retrieve_message -v

# Один файл
pytest tests/test_history.py -v

# По паттерну
pytest tests/ -k "history" -v
```

### Отладка тестов

```bash
# С выводом print
pytest tests/test_history.py -s

# С максимальной детализацией
pytest tests/test_history.py -vv

# Остановка на первой ошибке
pytest tests/ -x

# Запуск только упавших тестов
pytest --lf

# Интерактивная отладка
pytest tests/test_history.py --pdb
```

### Производительность

```bash
# Топ-10 самых медленных тестов
pytest tests/ --durations=10

# Параллельное выполнение (требует pytest-xdist)
pytest tests/ -n auto
```

---

## 🐛 Troubleshooting

### Проблема: ModuleNotFoundError

**Решение**:
```bash
export PYTHONPATH=.
pytest tests/
```

### Проблема: Тесты зависают

**Решение**: Используйте timeout
```bash
pytest tests/ --timeout=60
```

### Проблема: Ошибка при импорте env переменных

**Решение**: Убедитесь, что в `conftest.py` установлены dummy значения
```python
os.environ["TELEGRAM_BOT_TOKEN"] = "dummy_token"
```

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы с тестами:
1. Проверьте [Issues](https://github.com/Sin7009/MemeBrain/issues)
2. Создайте новый Issue с тегом `testing`
3. Напишите в [Discussions](https://github.com/Sin7009/MemeBrain/discussions)

---

## 📝 Changelog

### v1.0.0 (текущая версия)
- ✅ Комплексная тестовая инфраструктура
- ✅ 107+ тестов
- ✅ CI/CD pipeline с GitHub Actions
- ✅ Покрытие кода >80%
- ✅ Makefile и shell скрипты для удобства
- ✅ Документация по тестированию

---

<div align="center">

**Сделано с ❤️ для качественного кода**

</div>
